[project]
name = "ai-workspace-tools"
version = "0.2.0"
description = "Helper tools for the agent rules workspace"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "rich>=13",
]

[project.scripts]
rulesync = "rulesync.cli:main"
memento = "memento:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rulesync", "skills/memento"]
